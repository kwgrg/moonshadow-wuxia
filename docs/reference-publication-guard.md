# 只读参考与发布边界复核（2026-09-24）

用户再次明确原版目录只能参考，不能直接复制。项目约束保持：代码独立实现；美术、动画和声音独立制作；对白重新创作；原目录只读，不建立原资源导入器。本轮没有访问原目录或修改游戏内容。

## 当前文件审查

审查开始时237个Git受控文件，公开目录104个文件，其中40张PNG。40图均匹配来源清单SHA-256，并有制作记录。没有发现原包、地图、原音频、完整原脚本实体或原资源导入器的具体证据。原包名称出现在定位说明中，不是产品读取入口。

有界核验了忽略的开发文件：work/original-story-analysis.json是机制摘要和定位元数据；三份revise补丁脚本修改本项目JS/MJS，没有读取原包的行为。未运行这些脚本。早期参考摘要确有少量原地图格坐标，因此不能笼统称仓库没有原坐标；它们不是地图文件或供产品直接加载的布局。未删除正常开发资料，也未把忽略目录加入部署。

本轮不检查全部Git历史、全部未追踪截图内容、原版台词相似性、生成服务历史或PNG caBX声明。bedroom/npcs/props早期制作记录仍缺可直接复核的生成源绝对路径；已有文档状态不能被当成所有来源都获独立认证。

## 新增的工程检查

- Git受控和未忽略待添加文件采用当前源码/文档格式允许列表；原包、配置、音频和压缩格式不能混入，PNG只能位于已登记资源目录。忽略目录与历史Git对象不在这项扫描内。
- 扫描整个public目录，拒绝未支持的文件格式、符号链接和目录联接。不能把音频或压缩包放在assets之外绕过检查。文本须为UTF-8且无NUL，PNG须有正确签名，并继续核对文件清单、哈希、文档和上游输入。
- 普通test:assets允许诚实标记unconfirmed并报告缺口；test:assets:publish和部署入口遇到未确认来源或未确认上游时失败。
- Wrangler配置必须仍为当前纯静态配置，只发布./public。受控部署入口固定配置、资产目录和工作目录，关闭自动配置；只接受--dry-run，拒绝位置参数、--assets、--config、--cwd、--env等覆盖。直接手工调用Wrangler不在此入口保障范围。

这些检查能防止常见误混入，不识别所有经过改名或嵌入源码的内容，也不验证来源文档声明的真实性。对白、脚本和媒体的来源仍须人工审查，不把检查通过解释为授权或法律结论。

## 本轮验证

- 来源检查：104个公开文件、40项制作记录，52个负例通过；涵盖发布根联接、目录逃逸、发布树内非PNG媒体/原包、改名二进制、无效PNG签名、来源未确认、部署输入覆盖等。
- npm run deploy:check：受控入口正常执行Wrangler 4.130.0 dry-run，报告读取105个资产文件、无绑定，没有上传或云部署。
- npm run test:http：104个匿名游戏资源和13个不应公开的路径检查通过。没有新增认证或远端存档依赖。
- 独立复查实际调用覆盖参数时立即exit 1，未启动Wrangler；内存模拟确认索引中已暂存、磁盘已删除的禁用格式仍被拒绝。该索引案例为模拟，不宣称做了完整Git历史取证。
- CLI依据：项目已安装的wrangler deploy --help；官方 [命令索引](https://developers.cloudflare.com/workers/wrangler/commands/)。本轮未升级依赖。

没有修改游戏剧情、画面、交互或存档。保留无登录、本地存档和静态Cloudflare能力。全流程仍未完成，剩余内容见fullflow-completion.md；这次来源边界修正不是复刻完成声明。
